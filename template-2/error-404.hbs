{{!< default}}

<section class="sr-hero-outer">
  <div class="sr-hero-inner">
    <p class="sr-404">404</p>
    <h1 class="sr-hero-title">Page not found</h1>
    <p class="sr-hero-desc">Looks like this page doesn't exist. Head back and keep exploring.</p>
    <a class="sr-home-button" href="{{@site.url}}">Back to homepage</a>
  </div>
</section>

<style>
  .sr-hero-outer {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 60vh;
  }
  .sr-hero-inner {
    text-align: center;
    max-width: 640px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    border: 1px solid rgba(0, 0, 0, 0.04);
    border-radius: 16px;
    padding: 30px;
    margin-top: 50px;
  }
  .sr-404 {
    font-family: var(--gh-font-heading);
    font-size: clamp(8rem, 10vw, 14rem);
    line-height: 1;
    margin-bottom: 20px;
  }
  .sr-hero-title {
    font-size: clamp(2.3rem, 3vw, 4rem);
    margin-bottom: 40px;
  }
  .sr-hero-desc {
    font-size: clamp(1.3rem, 2vw, 2rem);
    line-height: 1.4;
    margin-bottom: 30px;
  }
  .sr-home-button {
    background-color: var(--ghost-accent-color);
    color: white;
    border: 0;
    border-radius: 100px;
    padding: .8em 1.4em;
    font-weight: 600;
    font-size: clamp(1.3rem, 2vw, 1.5rem);
  }
  .sr-home-button:hover {
    opacity: .95;
  }
  @media (max-width: 768px) {
    .sr-hero-inner {
      width: 75%;
    }
  }
</style>